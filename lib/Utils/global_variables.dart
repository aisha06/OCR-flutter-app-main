// We have created global variable for storing the text data from image
// So we can use this text anywhere in the code
import 'package:get/get_rx/src/rx_types/rx_types.dart';

/// Text String from scanned image
RxString textFromImage = ''.obs;
